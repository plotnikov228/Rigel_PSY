import 'package:flutter/material.dart';

class K32Page extends StatelessWidget {
  const K32Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
